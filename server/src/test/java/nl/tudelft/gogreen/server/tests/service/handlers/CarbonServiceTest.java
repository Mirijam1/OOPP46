package nl.tudelft.gogreen.server.tests.service.handlers;

import nl.tudelft.gogreen.server.service.handlers.CarbonService;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class CarbonServiceTest {
    @Test
    public void placeHolderTest() {
        //assertEquals(new CarbonService(null).fetchPoints(null), new Float(1F));
    }
}
